const validaToken = require('./validaToken');
const validaName = require('./validaName');
const validaIdade = require('./validadeIdade');
const validaSeTalkExiste = require('./validaSeTalkExiste');
const validaTalk = require('./validaTalk');

module.exports = {
    validaToken,
    validaName,
    validaIdade,
    validaSeTalkExiste,
    validaTalk,
};
