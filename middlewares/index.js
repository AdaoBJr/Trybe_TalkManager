const { talkerSearch,
  palestrantes, talkerId, loguinToken, post, putFunction, exclude } = require('./MiddleSearch');

module.exports = {
  talkerSearch,
  palestrantes,
  talkerId,
  loguinToken,
  post,
  putFunction,
  exclude,
};
